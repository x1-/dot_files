########################################
# aliases

alias la 'ls -a'
alias ll 'ls -lh'

alias rm 'rm -i'
alias cp 'cp -i'
alias mv 'mv -i'

alias mkdir 'mkdir -p'
alias vi "vim"

# sudo の後のコマンドでエイリアスを有効にする
alias sudo 'sudo '

alias emacs 'env TERM=xterm-256color /usr/local/bin/emacs'

# exa
alias le 'exa -bghHliS'
alias lax 'exa -abghHlS'
alias llx 'exa -abghHliS'
alias lll 'exa -abghHl'

